mailapi is a project for sending an email to user using API, and 2 type. normal sending email, and using template in sample-template directory.

# Usage
- Copy `sample-template` to `template` if want using template.